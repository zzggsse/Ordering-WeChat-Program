# 奶茶点单系统 · 技术设计文档（TDD）

> 版本：v1.0　|　类型：技术设计文档
> 关联产品文档：README.md（PRD）
> 技术选型：Java Spring Boot + 原生小程序 + Vue3 + Element Plus + MySQL + Redis + COS
> 部署：腾讯云服务器（CVM）Docker Compose（主），微信云开发作小程序侧轻量辅助

---

## 目录

1. [总体技术架构](#1-总体技术架构)
2. [技术栈总览](#2-技术栈总览)
3. [前端实现方案](#3-前端实现方案)
4. [后端实现方案（Spring Boot）](#4-后端实现方案spring-boot)
5. [数据库与缓存设计](#5-数据库与缓存设计)
6. [各功能模块的实现方案](#6-各功能模块的实现方案)
7. [关键业务流程实现](#7-关键业务流程实现)
8. [部署方案与运维](#8-部署方案与运维)
9. [开发规范与工程结构](#9-开发规范与工程结构)
10. [依赖与类库清单](#10-依赖与类库清单)
11. [课程项目落地（本地运行）](#11-课程项目落地本地运行不依赖腾讯云)

---

## 1. 总体技术架构

```
┌───────────────────────────┐    ┌───────────────────────────┐
│   顾客端小程序（原生）       │    │   商家端小程序（原生）       │
│   WXML/WXSS/JS + 微信API    │    │   核心功能：接单/出杯/叫号    │
└─────────────┬─────────────┘    └─────────────┬─────────────┘
              │  HTTPS / WebSocket               │  HTTPS / WebSocket
              ▼                                  ▼
┌───────────────────────────────────────────────────────────────┐
│                    微信小程序网关 / HTTPS                       │
│               （Nginx：静态资源 + 反向代理 + TLS）               │
└──────────────────────────────┬────────────────────────────────┘
                               ▼
┌───────────────────────────────────────────────────────────────┐
│              后端服务层  Java Spring Boot 3（单个服务先行）       │
│  Controller ─ Service ─ Mapper                          ┌─────┐│
│  安全认证 JWT · RBAC 权限 · 订单状态机 · 支付 · 叫号推送 · 报表  ││
│  WebSocket 服务（接单/出杯/叫号实时推送）                 │      ││
└───────┬───────────┬───────────┬──────────────┬──────────┐      │
        ▼           ▼           ▼              ▼          ▼
     ┌─────────┐ ┌─────────┐ ┌──────────────┐ ┌────────┐ ┌──────────┐
     │  MySQL   │ │  Redis   │ │ 腾讯云 COS   │ │ RabbitMQ│ │ 定时任务  │
     │ 主数据    │ │缓存/队列/锁│ │ 图片/素材存储 │ │ 削峰解耦│ │ 关单/报表 │
     └─────────┘ └─────────┘ └──────────────┘ └────────┘ └──────────┘
```

**架构要点**
- 单后端服务 + 三者互联；后续流量上来再拆订单/支付/报表为独立服务。
- 商家端小程序与 PC 共享同一套 API（同一后端、同一数据库），只是客户端不同，天然保证双端数据一致。
- 实时性用 WebSocket（叫号/接单）；异步提醒（顾客取餐通知）用微信订阅消息。
- 全部对公网只暴露 Nginx，后端不直接对外。

---

## 2. 技术栈总览

| 层 | 技术 | 用途 |
| --- | --- | --- |
| 顾客小程序 | 原生小程序（WXML/WXSS/JS）+ 微信开发者工具 | 点单、支付、订单查询、会员 |
| 商家小程序 | 原生小程序（核心功能） | 接单、出杯叫号、售罄、今日看板 |
| 商家 PC | Vue 3 + Element Plus + Pinia + Vue Router + Vite | 菜单/订单/库存/会员/营销/报表/权限全量管理 |
| 后端 | Java 17 + Spring Boot 3 + Spring Security + MyBatis-Plus | 业务逻辑、鉴权、状态机、报表 |
| 通信 | RESTful API + WebSocket + 微信订阅消息 | 数据交互与实时推送 |
| 主数据 | MySQL 8.0 | 订单、商品、用户、会员、库存等持久化 |
| 缓存/队列/锁 | Redis 7（Redis 分布式锁 / 延迟队列 / 缓存） | 鉴权、菜单缓存、库存预扣、限流、超时关单 |
| 消息 | RabbitMQ（可选，第二阶段引入） | 支付回调削峰、报表异步、事件解耦 |
| 存储 | 腾讯云 COS | 商品图、用户头像、上传素材 |
| 部署 | 腾讯云 CVM + Docker Compose + Nginx | 环境编排与反向代理 |

---


## 3. 前端实现方案

### 3.1 顾客端小程序（原生）

**技术组成**
- WXML（结构）+ WXSS（样式，rpx 自适应）+ JS（逻辑）/ JSON（配置）。
- 状态/数据管理：不引入重型框架，用页面数据 + `wx.setStorageSync/getStorageSync` 缓存登录态、购物车。
- 网络：封装 `wx.request` 统一拦截器（带 token、统一错误码、自动刷新登录态）。
- 实时推送：`wx.connectSocket`（WebSocket）+ `wx.requestSubscribeMessage`（订阅消息授权）。

**核心页面与实现**
| 页面 | 实现要点 |
| --- | --- |
| 首页/菜单 | 请求分类与商品接口；售罄商品置灰；图片走 COS CDN 加速。 |
| 商品详情/加购 | 选择规格（糖度/温度/小料/杯型），记录到本地购物车，计算小计与加价。 |
| 购物车/确认订单 | 组装订单草稿，携带备注、自取/外带方式，调下单接口。 |
| 支付 | 后端返回支付参数后调用 `wx.requestPayment`，支付回调由后端处理。 |
| 我的订单 | 查询订单列表/详情，状态实时更新（前端 WebSocket 或下拉刷新）。 |
| 取餐提醒 | 授权订阅消息 + WebSocket 双通道，收到状态变更即展示取餐号。 |

**微信能力调用清单**
- `wx.login` 获取 code → 后端换 openid 生成登录态。
- `button open-type="getPhoneNumber"` / 手机号快速验证组件 → 绑定手机号。
- `wx.requestPayment` 拉起支付。
- `wx.requestSubscribeMessage` 订阅取餐/优惠通知。
- `wx.connectSocket` 维持叫号长连接。
- `wx.chooseMedia / wx.uploadFile` 用户评价上传图片 → COS。

### 3.2 商家端小程序（原生，仅核心功能）

复用同一套原生小程序工程 + API 基座，只打包工作台相关页面：
- 工作台：今日营收/订单数/待接单 Widget。
- 接单/拒单：调订单接口 + WebSocket 监听新单。
- 出杯叫号：点击"出杯"→ 后端叫号并广播到排队屏（本机亦可作叫号屏）。
- 售罄开关、今日看板。

### 3.3 商家端 PC（Vue 3 + Element Plus）

**技术组成**
- Vue 3（Composition API）+ Vite 构建 + Vue Router + Pinia（状态）+ Element Plus（组件库）。
- Axios 封装统一请求、拦截器、token、错误提示；ECharts 做仪表盘/报表图表。
- 权限：登录后后端返回角色权限，前端按路由/按钮级 `v-permission` 控制显示。

**主要模块**
| 模块 | 组件与实现 |
| --- | --- |
| 仪表盘 | ECharts 趋势图、卡片统计、排行榜 |
| 订单管理 | Data Table + 筛选/分页、订单详情 Drawer、退款审核对话框 |
| 菜单管理 | 分类树、表单、上下架/售罄开关、规格编辑器、上传图片到 COS |
| 库存采购 | 库存表格、预警高亮、盘点、采购单表单 |
| 会员/营销 | CRUD 表单、优惠券模板配置、规则引擎展示 |
| 数据报表 | 多维度筛选 + ECharts 图表导出 |
| 员工与权限 | 用户管理、角色管理（RBAC）、权限树 |
| Deploy | Vite 构建产物由 Nginx 静态托管 |

---


## 4. 后端实现方案（Spring Boot）

### 4.1 分层结构（Controller → Service → Mapper）

```
com.tea.order
├─ config        # 安全、WebSocket、CORS、Redis、MyBatisPlus 配置
├─ controller    # REST API 入口
├─ service       # 业务逻辑（接口 + 实现）
├─ mapper        # MyBatis-Plus 数据访问
├─ entity / dto / vo  # 实体、出入参、视图对象
├─ security      # JWT 认证、UserDetails、权限注解
├─ websocket     # 接单/出杯/叫号推送
├─ job           # 定时任务（超时关单、报表、清缓存）
├─ common        # 统一返回、异常、常量、工具
└─ client        # 第三方：微信 SDK、COS、Redis
```

### 4.2 技术要点

| 主题 | 实现 |
| --- | --- |
| 框架 | Spring Boot 3.x，Java 17；spring-boot-starter-web、validation、webSocket、security、data-redis、mybatis-plus |
| ORM | MyBatis-Plus：实体 + ServiceImpl + CRUD；复杂报表用注解/XML SQL |
| 鉴权 | JWT（登录后发 token，Redis 存黑名单/会话）；Spring Security + 注解 `@PreAuthorize` |
| 参数校验 | `@Valid` + validator 统一异常处理 |
| 统一返回 | `ApiResponse<T>` 统一状态码，全局 `@RestControllerAdvice` 异常拦截 |
| 分布式锁 | Redisson（库存预扣、优惠券领取防并发） |
| 延迟任务 | Redis ZSet 延迟队列（订单超时未支付自动关闭）+ `@Scheduled` 扫描 |
| 支付 | 微信支付 V3 SDK：统一下单、回调验签、退款 |
| 实时推送 | spring-boot-starter-websocket，按门店/用户分组广播 |
| 第三方 | WxJava（weixin-java-miniapp、weixin-java-pay）、腾讯云 COS SDK、Lombok |

### 4.3 微信登录流程

```
小程序 wx.login 取 code
 → POST /api/auth/login { code, nickname, avatar }
 → 后端 code2session 换 openid/session_key（拼接 appid+secret，URL 请求微信）
 → 首次登录自动建用户，绑定 unionid
 → 生成 App 业务 JWT（含 userId、role、storeId）
 → 返回 token → 小程序存 Storage，后续请求头带 Authorization
```

---

## 5. 数据库与缓存设计

### 5.1 MySQL 核心表（主数据）

| 表 | 说明 |
| --- | --- |
| `user` | 顾客/账号，openid、unionid、手机号、状态 |
| `store` | 门店：名称、地址、营业时间、状态 |
| `category` | 商品分类 |
| `product` | 商品：名称、描述、基础价、图片、标签、排序、状态 |
| `product_spec` | 规格项：类型（糖度/温度/杯型）、选项名、加价 |
| `product_spec_opt` | 规格选项值与加价 |
| `cart` | 购物车（可选：仅本地存储时可不落表） |
| `order` | 订单主表：门店、状态机字段、总额、支付方式、队列号、取餐方式 |
| `order_item` | 订单明细：商品、规格快照、单价、数量 |
| `inventory` | 库存：原材料/成品数量、预警阈值 |
| `inventory_log` | 库存流水（出/入/盘点） |
| `member` / `member_account` | 会员等级、积分、储值账户 |
| `coupon` | 优惠券模板 |
| `user_coupon` | 用户持有的券 |
| `refund` | 退款记录 |
| `staff` | 员工账号 |
| `role` / `permission` / `role_permission` | RBAC 权限 |
| `sys_operation_log` | 操作日志审计 |

### 5.2 Redis 用途

| 用途 | 数据结构 | 说明 |
| --- | --- | --- |
| 会话/登录态 | String | token 黑名单、踢人下线 |
| 菜单缓存 | Hash / JSON | 商品+规格缓存，变更时失效重建 |
| 库存预扣 | String + 分布式锁 | 下单先扣 Redis 库存，订单支付后落库 |
| 订单超时关单 | ZSet | score=超时时间戳，扫描处理 |
| 叫号队列 | List / Stream | 当前排队与叫号顺序 |
| 限流 | 计数器 / Redisson RRateLimiter | 接口防刷、下单限流 |
| 秒杀/券防并发 | 分布式锁 | 优惠券领取、库存扣减幂等 |

### 5.3 腾讯云 COS 用途

- 商品图、分类图、品牌素材：前端预签名直传 COS（`PutObject`），DB 只存 key/URL。
- 用户头像、评价图片：`wx.uploadFile` → 后端生成预签名 URL 直传，避免大文件走业务服务器。
- 报表导出、打印模板等静态产物。

**注意**：COS 桶建议设为私有读 + CDN 鉴权访问，或对象公开只针对商品图（评估合规）。

---


## 6. 各功能模块的实现方案

### 6.1 顾客下单（含规格与购物车）
- 小程序读取菜单（带缓存）→ 选规格 → 本地购物车 → 提交 `POST /api/order`。
- 后端校验门店营业状态、商品上下架、规格合法、库存充足（Redis 预扣 + 红锁）。
- 生成订单与制作队列号，状态机：`CREATED(待支付)`。
- 购物车本地存（`wx.setStorageSync`），DB 不落 `cart`，减轻写压力。

### 6.2 微信支付
- 后端 `POST /api/order/{id}/pay` → 使用 WxJava 统一下单（JSAPI）→ 返回 `timeStamp/nonceStr/package/signType/paySign`。
- 小程序 `wx.requestPayment` 拉起收银台 → 支付成功后微信回调后端 `notify_url`（验签 + 幂等）。
- 回调更新订单为已支付、扣减库存（Redis 二次确认）、发放积分/储值抵扣、触发接单推送。
- 退单：后端调微信退款接口，记录 `refund` 表，原路退款。

### 6.3 订单状态机与取餐叫号
状态：`待支付 CREATED → 已支付 PAID/待制作 → 制作中 MAKING → 待取餐 READY → 已完成 DONE`，外加 `已取消 / 已退款`。
- 用枚举 + `@Transactional` 保证流转合法，变更写状态日志表（支持追溯与消息推送）。
- 店员"出杯"调用 `POST /api/order/{id}/ready` → 状态转 READY → WebSocket 广播叫号 + 推送订阅消息。
- 顾客小程序"我的订单"通过 WebSocket 实时刷新 → 展示取餐号与大屏同步。

### 6.4 WebSocket 实时推送（接单 / 出杯 / 叫号 / 顾客取餐提醒）
- `@ServerEndpoint` 或 `TextWebSocketHandler`，建立连接时带 token → 校验后按 `storeId`/`userId` 分组（ConcurrentHashMap 保存会话）。
- 事件发布走 Spring事件 + Redis 广播（多实例时）或直接内存广播（单实例先做内存即可）。
- 消息体统一：`{ type, orderId, status, queueNo, timestamp }`。
- **大屏叫号**：门店排队屏也是同一个 WebSocket 客户端，收到 READY 事件播放语音/滚动号码。

### 6.5 库存联动（售罄 → 扣减 → 预警）
- 售罄：`PUT /api/store/out-of-stock` 写 status，同时失效商品缓存；顾客端实时置灰。
- 下单扣减：Redis 预扣 → 支付成功落库 `inventory_log`；退款回滚。
- 预警：定时扫描低于阈值的原材料 → 生成 `inventory_log` 预警记录 → 通知 PC 端（WebSocket）→ 可一键生成采购单。

### 6.6 会员 / 储值 / 优惠券
- 会员：`member` 等级 + 积分规则引擎；消费/好评累计积分，等级触发折扣（服务端计算），DB 记录流水。
- 储值：`member_account` 余额 + 流水，支付时可抵用；充值要过支付 + 合规评估。
- 优惠券：模板（满减/折扣/限定商品）→ 领取锁（分布式锁防超发）→ 订单下单时校验可用并锁定 → 支付成功后核销。

### 6.7 退款流程
- 顾客申请 → 状态转 `REFUND_APPLY` → 门店 PC/商家端审核。
- 通过：调微信退款 → 成功写 `refund` → 状态 `REFUNDED`，库存回滚，积分/储值回退。
- 拒绝：回填原因，状态回 `PAID`，通知顾客。
- 全程计入操作日志与财务对账。

### 6.8 报表与数据分析
- 页面实时统计走 SQL（`GROUP BY` 聚合 order/order_item）。
- 大查询优化：天级聚合表 `report_daily` 定时任务生成，报表读聚合表。
- ECharts 前端渲染；支持导出 CSV（后端流式生成）。

### 6.9 权限体系（RBAC）
- `staff/role/permission` 三表；Spring Security + `@PreAuthorize`。
- 判断归属：所有写操作校验 `storeId` 属于当前账号，避免越权。
- 前端 PC 按角色渲染菜单与按钮；商家小程序按角色返回可用能力。

### 6.10 文件上传（COS）
- 后端 `POST /api/upload/presign` 返回 COS 预签名 URL + key。
- 前端直接 PUT/直传 COS，成功后表单只提交 key/URL，DB 记录。
- 提供公开读 CDN 用于商品图展示。

---

## 7. 关键业务流程实现

### 7.1 下单 + 支付 + 接单 + 出杯取餐（跨端时序）

```
顾客小程序            后端 Spring Boot              MySQL          Redis        微信/被订阅
   │ 登录(login)      │                              │             │              │
   │─────────────────>│  code2session -> JWT         │             │              │
   │ <── token ────── │                              │             │              │
   │ 下单(规格)       │─────────────────────────────>│ 校验/建单    │ 预扣库存      │
   │ <── orderId ──── │                              │             │              │
   │ requestPayment   │                              │             │              │
   │─────────────────>│  JSAPI 下单                   │             │             │微信支付
   │ <── payParams ── │                              │             │              │
   │ 收银台... 支付    │                              │             │              │
   │  <──支付回调──────><-- 验签/幂等 --> 状态=PAID    │ 落库扣库存   │ 预扣转实扣    │
   │                  │ 发布接单事件 ──WebSocket──> 商家小程序接单
   │                  │ 商家点击出杯 READY ──广播──> 大屏叫号 + 订阅消息
   │ 收到READY/取餐    │
```

### 7.2 超时未支付自动关单
- 下单时写入 Redis ZSet（`order:pay:delay`，score=15min 后时间戳）。
- `@Scheduled` 每 30s 扫描到期 item → 校验订单仍待支付 → 置状态`CANCELLED`、释放 Redis 库存、通知前端失效。

---

## 8. 部署方案与运维

> 课程项目请直接使用**第 11 节（本地运行，无需腾讯云）**。第 8 节的腾讯云方案为生产可选版，非必须。

### 8.1 推荐：腾讯云 CVM + Docker Compose

```
腾讯云 CVM（2C4G 起步，后续可升配）
├─ docker-compose.yml 编排：
│   ├─ mysql:8.0  （数据卷持久化，定时备份）
│   ├─ redis:7
│   ├─ rabbitmq（可选，第二阶段）
│   └─ app（Spring Boot 镜像，连上述容器）
└─ Nginx：
    ├─ 静态托管 PC 管理端（Vue 产物）
    ├─ 反向代理 /api、/ws 到后端
    └─ TLS 证书（HTTPS，小程序强制要求）
```

**上线运维要点**
- HTTPS 必须（小程序要求合法域名 + TLS）；Nginx 配置证书。
- 小程序后台配置合法 request/socket/uploadFile 域名，指到你的域名。
- MySQL 每日备份（cron + mysqldump，可上传 COS 保留多份）。
- Redis 持久化 AOF；连接设置密码。
- 日志采集到统一文件 + 定时清理；监控用腾讯云基础告警。


> **纯 Docker 即可**：本方案不依赖任何云开发平台。以下为可直接落地的编排示例。

**8.1.1 完整 docker-compose.yml（示例）**

```yaml
services:
  mysql:
    image: mysql:8.0
    container_name: tea-mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: tea_order
      TZ: Asia/Shanghai
    volumes:
      - ./data/mysql:/var/lib/mysql
    ports:
      - "3306:3306"

  redis:
    image: redis:7
    container_name: tea-redis
    restart: always
    command: ["redis-server", "--appendonly", "yes", "--requirepass", "${REDIS_PASSWORD}"]
    volumes:
      - ./data/redis:/data
    ports:
      - "6379:6379"

  app:
    image: tea-order-backend:latest
    container_name: tea-app
    restart: always
    depends_on:
      - mysql
      - redis
    environment:
      SPRING_PROFILES_ACTIVE: prod
      DB_HOST: mysql
      REDIS_HOST: redis
      WX_APPID: ${WX_APPID}
      WX_SECRET: ${WX_SECRET}
      WX_PAY_MCHID: ${WX_PAY_MCHID}
      COS_SECRET_ID: ${COS_SECRET_ID}
      COS_SECRET_KEY: ${COS_SECRET_KEY}
    ports:
      - "8080:8080"

  nginx:
    image: nginx:1.27
    container_name: tea-nginx
    restart: always
    depends_on:
      - app
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/html:/usr/share/nginx/html        # PC 管理端(Vue)静态产物
      - ./nginx/cert:/etc/nginx/cert:ro           # TLS 证书
    ports:
      - "80:80"
      - "443:443"
```

```bash
# 使用方式（在服务器上）
cp .env.example .env        # 填入密钥
docker compose up -d        # 一键启动全部容器
docker compose logs -f app  # 查看后端日志
```

> 说明：`tea-order-backend` 镜像由后端项目 `docker build` 生成；Nginx 同时做 PC 端静态托管与 `/api`、`/ws` 反向代理，需配置 HTTPS 证书满足小程序要求。
### 8.2 备选：微信云开发 / 云托管
- 仅当希望减少运维：用云托管（CloudBase Run）直接跑 Spring Boot 的 Docker 镜像，数据库用云数据库。
- 局限：常驻 Java 进程按量计费在运行时间长时可能更贵；WebSocket 长连接建议放后端由云托管负载解决或仍用 CVM。**本方案仅作可选项，不推荐为主方案。**

### 8.3 环境区分
- 本地（dev）→ 测试（test）→ 生产（prod）三套配置；`application-{env}.yml` + Nacos/环境变量注入密钥（不写进仓库）。

---

## 9. 开发规范与工程结构

| 规范 | 说明 |
| --- | --- |
| 接口风格 | RESTful，统一 `/api/v1/...`，统一 `ApiResponse` 返回体 |
| 版本管理 | Git + 分支（feature/dev/release/master），Semantic Versioning |
| 命名 | 实体/表名 snake_case；接口与表一一对应 |
| 安全 | 密钥走环境变量/配置中心，不提交；JWT 过期鉴权；支付回调验签 |
| 数据库变更 | Flyway 管理表结构版本 |
| 测试 | 单元测试（Spring Boot Test）+ 关键链路集成测试 + 小程序真机冒烟 |
| 接口文档 | OpenAPI（springdoc）自动生成，配合 swagger-ui |
| 前后端协作 | 统一字段命名 + 分页/异常约定，先定 OpenAPI 再开发 |

## 10. 依赖与类库清单

| 分类 | 依赖 |
| --- | --- |
| 框架 | spring-boot-starter-web / security / websocket / validation / data-redis / aop |
| ORM | mybatis-plus-boot-starter；flyway-core（数据库迁移） |
| 鉴权 | jjwt（JWT 生成校验） |
| 分布式锁 | redisson（配合 Redis） |
| 微信 | weixin-java-miniapp、weixin-java-pay |
| 云存储 | com.qcloud:cos_api（腾讯云 COS SDK） |
| 工具 | lombok、mapstruct、hutool、easyexcel（报表导出） |
| 数据库驱动 | mysql-connector-j |
| 接口文档 | springdoc-openapi |
| 前端 PC | vue3、vite、pinia、vue-router、element-plus、axios、echarts |
| 小程序 | 原生 + 微信基础库（官方 API） |

---

> 本文档为技术选型与实现方案说明，随需求评审后与 PRD 对齐，落地时以具体迭代拆分为准。




---

## 11. 课程项目落地（本地运行，不依赖腾讯云）

> 本单据你为课程/毕设项目的定位，第 8 节腾讯云方案不再作为必需步骤，改为按需可选项。

### 11.1 运行方式（全部本机）

| 组件 | 课程项目做法 |
| --- | --- |
| MySQL / Redis | 本机安装即可；或本地 Docker 仅起这两个容器（`docker run` 单命令） |
| 后端 | 本地 `mvn spring-boot:run`，连本机 MySQL/Redis，端口 8080 |
| PC 管理端 | `npm run dev` → `http://localhost:5173`，代理 `/api` 到 8080 |
| 顾客/商家小程序 | 微信开发者工具导入工程，开发者工具 + 真机预览调试 |

### 11.2 微信小程序连接本机后端
- 开发者工具勾选"不校验合法域名、web-view、TLS 版本以及 HTTPS 证书"，即可用 `http://localhost/api` 调试。
- 真机调试需要局域网地址或内网穿透（如 frp / ngrok）暴露 8080，并临时把 baseURL 指过去。
- 不申请云开发、不买服务器、不配域名即可完成演示。

### 11.3 微信支付 → 模拟支付（无商户号时）
真实微信支付需要企业主体 + 商户号，个人课程项目没有。改为：
- 后端保留**统一下单与回调的接口骨架**，但加一个 `mock` 开关（配置项 `pay.mock=true`）。
- mock 开启时：`发起支付`接口直接返回成功，第三方回调用一个 `POST /api/pay/mock-notify` 模拟到账，订单置"已支付"。
- 借此在课程里演示"支付 → 接单 → 出杯 → 取餐"完整链路，同时代码预留真实支付切换点。

### 11.4 图片存储 COS → 本地文件
不引入腾讯云对象存储：
- 后端提供 `POST /api/upload` 保存到本地 `./uploads/` 目录，DB 存相对路径（如 `/uploads/xxx.jpg`）。
- 后端配置静态资源映射把 `/uploads/**` 映射到该目录供前端访问。
- 代码中把"存储"抽象成接口（`FileStorage`），本地实现 + 预留 COS 实现对课程展示有加分。

### 11.5 订阅消息 / WebSocket 备选
- WebSocket 叫号、接单推送照常实现（本地即可稳定演示）。
- 微信订阅消息如不想申请模板，可将到店提醒做成应用内及时刷新 / 站内信，讲解时说明真实场景会接订阅消息。

### 11.6 课程交付建议
- 接口文档（OpenAPI）+ 数据库建表脚本（`schema.sql`）+ docker 可选脚本放在 `deploy/`。
- 关键链路写测试：下单、模拟支付、状态流转、退款；用 H2 内存库跑单测，减少环境依赖。
- 演示脚本一页梳理：顾客下单 → 模拟支付 → 商家接单 → 出杯叫号 → 取餐 → 退款，完整走通即可。

---

> 本文档为课程项目技术方案，以本地可直接运行优先；生产化（腾讯云/真实支付）作为可选演进方向。


---

## 12. 已实现要点（与早期方案的差异 / 补充）

- 手机号验证码登录走短信 mock（固定验证码 `123456`），与微信登录并存。
- 角色收敛为 **顾客 / 店员 / 店长** 三级；JWT 中角色映射店员、店长为 `ROLE_STAFF`，店长为 `ROLE_OWNER`，工作台接口按 `hasAnyRole('ADMIN','STAFF')`、员工管理按 `hasRole('OWNER')` 鉴权。
- 商品接口拆分：
  - 顾客端 `/api/v1/products` 默认只返回在售商品（新增默认 `status=1`）。
  - 商家工作台 `/api/v1/admin/products` 全量返回（含下架），供上下架 / 售罄 / 单品库存管理。
- 点单 / 首页数据在 `onShow` 实时拉取，下架 / 售罄即时生效；下单在 `OrderService.create` 二次校验 `status!=1`（已下架）、`soldout==1`（已售罄）。
- 订单状态机补充：READY 后由顾客 `POST /orders/{id}/receive` 确认完成；超时 2 小时未确认由 `@Scheduled` 自动完成；取餐号数值升序展示。
- 积分 / 会员（`PointsService`）：普通 1:1、黄金（累计消费满 500）1:1.2、钻石（单次充值满 500）1:2；充值送积分；积分兑换优惠券（`coupon.points_cost` 150 起步、`once_per_user` 限领）；已领过的限领券在兑换列表隐藏。
